[pytest]
addopts = -ra -q --strict-markers --strict-config
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: integration tests that require a running Atlas Command server
